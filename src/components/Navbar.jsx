import { AppBar, Toolbar, Typography } from "@mui/material"

const Navbar=()=>{
    return(
        <AppBar>
            <Toolbar>
                <Typography>HEllo shahriar</Typography>
            </Toolbar>
        </AppBar>
    )
}
export default Navbar